{
  "name": "fedora-firewall-control-web",
  "version": "0.1.2",
  "private": true,
  "description": "Local read-only dashboard for Fedora Firewall Control",
  "type": "module",
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "check": "node --test",
    "test": "node --test"
  }
}
