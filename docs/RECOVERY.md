# Recovery

Version 0.1.0 makes no changes, so exiting it requires no recovery action.

For future releases, keep local console access before firewall changes. Panic mode must always display the recovery command before activation, expected to be `sudo ffc --panic-off`. No automatic reload or permanent configuration update is permitted.
